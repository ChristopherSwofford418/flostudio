import { useEffect, useMemo, useState } from 'react'
import { useNavigate, useSearchParams } from 'react-router-dom'
import Layout from '../components/Layout'
import { useWorkspace } from '../context/WorkspaceContext'
import { supabase } from '../supabase'
import { appInsightsPath, appStoreConnectPath } from '../lib/appStoreConnectRouting'

async function ascRequest(payload) {
  const { data: sessionData } = await supabase.auth.getSession()
  const token = sessionData?.session?.access_token
  if (!token) throw new Error('Sign in again before opening App Insights.')
  const response = await fetch('/api/app-store-connect', { method:'POST', headers:{ 'Content-Type':'application/json', Authorization:`Bearer ${token}` }, body:JSON.stringify(payload) })
  const data = await response.json().catch(() => ({}))
  if (!response.ok) throw new Error(data.error || 'FloStudio could not load App Store Connect data.')
  return data
}

function StatusCard({ label, value, note, highlight = false, compactValue = false }) {
  return <div className="studio-panel insights-status-card" style={{ padding:17, background:highlight ? 'linear-gradient(145deg,rgba(62,62,62,.56),rgba(22,22,22,.76))' : 'rgba(26,26,26,.42)', borderColor:highlight ? 'rgba(197,197,197,.36)' : 'rgba(240,240,240,.12)' }}><div className="studio-kicker" style={{ color:highlight ? 'var(--signal)' : 'rgba(240,240,240,.55)' }}>{label}</div><div className="insights-status-card__value" title={String(value)} style={{ color:'#ffffff', fontSize:compactValue ? 16 : 23, fontWeight:850, letterSpacing:compactValue ? '-.025em' : '-.05em', marginTop:8 }}>{value}</div>{note && <p className="insights-status-card__note" style={{ color:'rgba(240,240,240,.58)', fontSize:10.5, lineHeight:1.45, marginTop:6 }}>{note}</p>}</div>
}

function availabilityLabel(entry) {
  if (!entry) return 'Not available yet'
  if (entry.status === 'not connected') return 'Not connected'
  if (entry.status === 'vendor number needed') return 'Vendor number needed'
  if (entry.status === 'available') return 'Available'
  if (entry.status === 'requires_vendor_number') return 'Vendor number needed'
  if (entry.status === 'requires_sales_or_analytics_report') return 'Apple report needed'
  if (entry.status === 'requires_analytics_report') return 'Analytics report needed'
  if (entry.status === 'unavailable') return 'Apple report unavailable'
  if (entry.status === 'no_sales_in_recent_periods') return 'No recent sales rows'
  if (entry.status === 'requested' || entry.status === 'pending') return 'Apple reports pending'
  if (entry.status === 'requires_admin_analytics_request') return 'Admin key needed once'
  if (entry.status === 'not_authorized') return 'Apple key access needed'
  if (entry.status === 'not_generated') return 'Apple report not generated'
  if (entry.status === 'partial') return 'Partially available'
  return 'Not authorized or unavailable'
}

function formatDate(value, { includeTime = false } = {}) {
  if (!value) return 'Not supplied'
  const date = new Date(value)
  if (Number.isNaN(date.getTime())) return String(value)
  return includeTime ? date.toLocaleString() : date.toLocaleDateString()
}

function boolLabel(value, truthy = 'Enabled', falsy = 'Not enabled') {
  if (value === null || value === undefined) return 'Not supplied'
  return value ? truthy : falsy
}

function formatAppleState(value) {
  if (!value) return '—'
  return String(value).split('_').filter(Boolean).map(part => `${part.slice(0, 1)}${part.slice(1).toLowerCase()}`).join(' ')
}

function formatApplePlatform(value) {
  if (!value) return 'Apple version resource'
  if (String(value).toUpperCase() === 'IOS') return 'iOS'
  return String(value)
}

function AvailabilityBadge({ entry }) {
  const available = entry?.status === 'available'
  return <span className={`insights-availability ${available ? 'is-available' : ''}`}>{availabilityLabel(entry)}</span>
}

function formatUnits(value) {
  return new Intl.NumberFormat('en-US', { maximumFractionDigits:2 }).format(Number(value || 0))
}

function formatUsd(value) {
  return new Intl.NumberFormat('en-US', { style:'currency', currency:'USD', maximumFractionDigits:2 }).format(Number(value || 0))
}

function formatApplePrice(price) {
  if (!price || price.customerPrice === null || price.customerPrice === undefined) return '—'
  const currency = String(price.currency || 'USD').toUpperCase()
  try {
    return new Intl.NumberFormat('en-US', { style:'currency', currency, maximumFractionDigits:2 }).format(Number(price.customerPrice))
  } catch {
    return `${price.customerPrice} ${currency}`
  }
}

function proceedsSummary(sales) {
  const entries = Object.entries(sales?.proceedsByCurrency || {})
  if (!entries.length) return { value:'—', note:sales?.message || 'Apple has not provided a Sales and Trends report.' }
  const [currency, amount] = entries[0]
  const value = new Intl.NumberFormat('en-US', { style:'currency', currency:currency === 'UNKNOWN' ? 'USD' : currency, maximumFractionDigits:2 }).format(Number(amount || 0))
  return { value, note:entries.length > 1 ? `Estimated developer proceeds by Apple currency: ${entries.map(([code, total]) => `${code} ${Number(total).toFixed(2)}`).join(' · ')}` : `Estimated developer proceeds in ${currency}.` }
}

export default function AppInsights() {
  const navigate = useNavigate()
  const [searchParams] = useSearchParams()
  const { apps, activeApp, setActiveApp } = useWorkspace()
  const [selectedId, setSelectedId] = useState('')
  const [fleet, setFleet] = useState([])
  const [fleetLoading, setFleetLoading] = useState(false)
  const [connection, setConnection] = useState(null)
  const [vendorInput, setVendorInput] = useState('')
  const [state, setState] = useState({ loading:true, syncing:false, error:'', notice:'' })
  const selectedApp = useMemo(() => apps.find(app => app.id === selectedId) || null, [apps, selectedId])
  const metrics = connection?.metrics || {}
  const sales = metrics.sales || null
  const analytics = metrics.analytics || null
  const analyticsReady = analytics?.status === 'available'
  const subscriptions = metrics.subscriptions || null
  const subscriptionsReady = subscriptions?.status === 'available'
  const configuration = metrics.configuration || null
  const configurationReady = configuration?.status === 'available'
  const proceeds = proceedsSummary(sales)
  const sourceFacts = selectedApp?.sourceFacts || selectedApp?.source_facts || {}
  const metadata = metrics.storeMetadata || null
  const release = metrics.release || null
  const testFlight = release?.testFlight || null
  const reportCoverage = analytics?.reportCoverage || {}
  const metricAvailable = key => analyticsReady && (analytics?.metricAvailability?.[key] || 'available') === 'available'
  const pricing = configuration?.pricing || null
  const appAvailability = configuration?.availability || null
  const requestedId = searchParams.get('app') || ''
  const analyticsHasActivity = analyticsReady && [analytics.firstTimeDownloads, analytics.redownloads, analytics.impressions, analytics.productPageViews, analytics.updates].some(value => Number(value || 0) > 0)
  const fleetSummary = useMemo(() => ({
    total:apps.length,
    connected:fleet.filter(item => item.connection?.status === 'connected').length,
    analyticsReady:fleet.filter(item => item.connection?.metrics?.analytics?.status === 'available').length,
    salesReady:fleet.filter(item => item.connection?.metrics?.sales?.status === 'available').length,
    missing:fleet.filter(item => item.connection?.status !== 'connected').length,
  }), [apps.length, fleet])

  useEffect(() => {
    const requested = apps.some(app => app.id === requestedId) ? requestedId : ''
    const preferred = requested || activeApp?.id || apps[0]?.id || ''
    if (preferred && (!selectedId || requested)) {
      setSelectedId(preferred)
      const app = apps.find(item => item.id === preferred)
      if (app) setActiveApp(app)
    }
  }, [apps, activeApp?.id, requestedId, selectedId, setActiveApp])

  useEffect(() => {
    if (!apps.length) { setFleet([]); return }
    let alive = true
    setFleetLoading(true)
    Promise.all(apps.map(async app => {
      try { const data = await ascRequest({ action:'status', productId:app.id }); return { app, connection:data.connection || null } }
      catch (error) { return { app, connection:null, error:error.message || 'Status unavailable' } }
    })).then(rows => { if (alive) setFleet(rows) }).finally(() => { if (alive) setFleetLoading(false) })
    return () => { alive = false }
  }, [apps])

  useEffect(() => {
    if (!selectedId) { setConnection(null); setVendorInput(''); setState(current => ({ ...current, loading:false })); return }
    let alive = true
    setState({ loading:true, syncing:false, error:'' })
    ascRequest({ action:'status', productId:selectedId })
      .then(data => { if (alive) { setConnection(data.connection || null); setVendorInput(data.connection?.vendor_number || '') } })
      .catch(error => { if (alive) setState({ loading:false, syncing:false, error:error.message }) })
      .finally(() => { if (alive) setState(current => ({ ...current, loading:false })) })
    return () => { alive = false }
  }, [selectedId])

  const selectApp = id => {
    setSelectedId(id)
    const app = apps.find(item => item.id === id)
    if (app) setActiveApp(app)
    navigate(appInsightsPath(id), { replace:true })
  }
  const sync = async () => {
    if (!selectedApp) return
    setState(current => ({ ...current, syncing:true, error:'', notice:'' }))
    try {
      const data = await ascRequest({ action:'sync', productId:selectedApp.id })
      const refreshed = { status:'connected', metrics:data.metrics, last_synced_at:data.syncedAt }
      setConnection(current => ({ ...current, ...refreshed }))
      setFleet(current => current.map(item => item.app.id === selectedApp.id ? { ...item, connection:{ ...item.connection, ...refreshed } } : item))
      setState(current => ({ ...current, notice:'Apple reporting data refreshed for the selected app.' }))
    } catch (error) { setState(current => ({ ...current, error:error.message })) }
    finally { setState(current => ({ ...current, syncing:false })) }
  }

  const saveVendorAndSync = async () => {
    if (!selectedApp || !vendorInput.trim()) { setState(current => ({ ...current, error:'Enter the Vendor Number shown in App Store Connect → Reports.' })); return }
    setState(current => ({ ...current, syncing:true, error:'', notice:'' }))
    try {
      await ascRequest({ action:'update_vendor_number', productId:selectedApp.id, vendorNumber:vendorInput.trim() })
      const data = await ascRequest({ action:'sync', productId:selectedApp.id })
      const refreshed = { vendor_number:vendorInput.trim(), status:'connected', metrics:data.metrics, last_synced_at:data.syncedAt }
      setConnection(current => ({ ...current, ...refreshed }))
      setFleet(current => current.map(item => item.app.id === selectedApp.id ? { ...item, connection:{ ...item.connection, ...refreshed } } : item))
      setState(current => ({ ...current, notice:'Vendor Number saved for this app and its latest Apple report was requested.' }))
    } catch (error) { setState(current => ({ ...current, error:error.message })) }
    finally { setState(current => ({ ...current, syncing:false })) }
  }

  if (!apps.length) return <Layout title="App Insights"><div className="page-shell" style={{ padding:'32px 30px' }}><section className="studio-panel" style={{ padding:30, textAlign:'center' }}><div className="studio-kicker">APP INSIGHTS</div><h1 style={{ color:'#ffffff', marginTop:8 }}>Add a portfolio app first.</h1><p style={{ color:'rgba(240,240,240,.62)', marginTop:8 }}>App Insights is always scoped to a real portfolio app, never a generic account.</p><button onClick={() => navigate('/portfolio')} className="studio-button" style={{ marginTop:16 }}>Go to portfolio →</button></section></div></Layout>

  return <Layout title="App Insights"><div className="page-shell app-insights-page" style={{ padding:'28px 30px 52px' }}>
    <section className="studio-dark abundance-hero flo-dark-surface" style={{ padding:'27px 30px', position:'relative', overflow:'hidden' }}><div style={{ position:'relative', zIndex:1, display:'flex', justifyContent:'space-between', gap:18, alignItems:'end', flexWrap:'wrap' }}><div><div className="studio-kicker">MANAGE / APP INSIGHTS</div><h1 className="studio-display" style={{ color:'#ffffff', fontSize:'clamp(32px,4vw,54px)', marginTop:9 }}>Apple performance, <span className="studio-serif" style={{ color:'var(--signal)' }}>one app at a time.</span></h1><p style={{ color:'rgba(240,240,240,.68)', fontSize:13, maxWidth:660, lineHeight:1.6, marginTop:10 }}>FloStudio shows only data returned by the connected App Store Connect key for the portfolio app selected below. Missing reports remain visibly unavailable—not estimated.</p></div><div style={{ minWidth:250 }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>ACTIVE APP</div><select value={selectedId} onChange={event => selectApp(event.target.value)} style={{ width:'100%', marginTop:7 }}>{apps.map(app => <option key={app.id} value={app.id}>{app.name}</option>)}</select></div></div></section>
    <section className="studio-panel" style={{ marginTop:16, padding:20, borderColor:'rgba(197,197,197,.25)' }}><div style={{ display:'flex', justifyContent:'space-between', gap:16, alignItems:'start', flexWrap:'wrap' }}><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>PORTFOLIO CONNECTION HEALTH</div><h2 style={{ color:'#fff', fontSize:22, marginTop:6 }}>What is live across all {fleetSummary.total} apps.</h2><p style={{ color:'rgba(240,240,240,.6)', fontSize:11.5, lineHeight:1.55, marginTop:5, maxWidth:720 }}>This view checks each app’s own encrypted App Store Connect record. A zero is shown only when Apple returned a report with zero activity; an unavailable report remains labeled with its exact Apple readiness state.</p></div><div style={{ display:'grid', gridTemplateColumns:'repeat(4,minmax(74px,1fr))', gap:8, minWidth:330 }}>{[['CONNECTED',fleetSummary.connected],['ANALYTICS',fleetSummary.analyticsReady],['SALES READY',fleetSummary.salesReady],['NEEDS KEY',fleetSummary.missing]].map(([label,value]) => <div key={label} style={{ padding:'9px 10px', border:'1px solid rgba(240,240,240,.13)', background:'rgba(18,18,18,.35)' }}><div className="studio-kicker" style={{ color:'rgba(240,240,240,.48)', fontSize:8 }}>{label}</div><b style={{ display:'block', color:'#fff', fontSize:20, marginTop:4 }}>{fleetLoading ? '—' : value}</b></div>)}</div></div><div style={{ display:'grid', gridTemplateColumns:'repeat(auto-fill,minmax(196px,1fr))', gap:8, marginTop:15 }}>{fleet.map(item => { const connected = item.connection?.status === 'connected'; const analyticsStatus = item.connection?.metrics?.analytics?.status || 'not connected'; const salesStatus = item.connection?.metrics?.sales?.status || 'vendor number needed'; return <button key={item.app.id} type="button" onClick={() => selectApp(item.app.id)} style={{ textAlign:'left', padding:'10px 11px', cursor:'pointer', border:`1px solid ${item.app.id === selectedId ? 'rgba(197,197,197,.62)' : 'rgba(240,240,240,.12)'}`, background:item.app.id === selectedId ? 'rgba(197,197,197,.11)' : 'rgba(18,18,18,.28)', color:'#fff' }}><div style={{ display:'flex', justifyContent:'space-between', gap:8 }}><b style={{ fontSize:11.5 }}>{item.app.name}</b><span style={{ color:connected ? 'var(--signal)' : '#d7d7d7', fontSize:9, fontWeight:850, textTransform:'uppercase' }}>{connected ? 'Connected' : 'Needs key'}</span></div><p style={{ color:'rgba(240,240,240,.56)', fontSize:9.5, lineHeight:1.45, marginTop:5 }}>Analytics: {availabilityLabel({ status:analyticsStatus })}<br/>Sales: {availabilityLabel({ status:salesStatus })}</p></button> })}</div></section>
    {state.loading ? <section className="studio-panel" style={{ marginTop:16, padding:28, color:'rgba(240,240,240,.65)' }}>Loading this app’s secured reporting state…</section> : !connection?.status || connection.status !== 'connected' ? <section className="studio-panel" style={{ marginTop:16, padding:26, borderColor:'rgba(197,197,197,.3)', background:'rgba(49,49,49,.3)' }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>APP STORE CONNECT NOT YET CONNECTED</div><h2 style={{ color:'#ffffff', fontSize:23, marginTop:7 }}>{selectedApp?.name} has no performance feed yet.</h2><p style={{ color:'rgba(240,240,240,.66)', fontSize:12, lineHeight:1.6, marginTop:8, maxWidth:700 }}>Open the portfolio connection panel, enter this app’s App Store Connect App ID and API Key ID, then drop the matching `.p8` file. FloStudio will validate the key, encrypt it server-side, and begin showing only authorized results here.</p><button onClick={() => navigate(appStoreConnectPath(selectedApp?.id))} className="studio-button" style={{ marginTop:15 }}>Connect {selectedApp?.name || 'this app'} →</button></section> : <>
      {analyticsReady ? <section className="insights-primary-grid" style={{ display:'grid', gridTemplateColumns:'repeat(3,minmax(0,1fr))', gap:12, marginTop:16 }}><StatusCard label="FIRST-TIME DOWNLOADS" value={metricAvailable('downloads') ? formatUnits(analytics.firstTimeDownloads) : '—'} note={metricAvailable('downloads') ? `Downloads report · last ${analytics.periodDays} days · processed ${formatDate(reportCoverage.downloads?.processingDate)}` : reportCoverage.downloads?.message || metrics.availability?.downloads?.message || 'Apple Downloads report required'} highlight /><StatusCard label="REDOWNLOADS" value={metricAvailable('redownloads') ? formatUnits(analytics.redownloads) : '—'} note={metricAvailable('redownloads') ? `Downloads report · last ${analytics.periodDays} days` : reportCoverage.downloads?.message || 'Apple Downloads report required'} /><StatusCard label="CONVERSION RATE" value={metricAvailable('conversionRate') && analytics.conversionRate !== null ? `${analytics.conversionRate}%` : '—'} note={metricAvailable('conversionRate') ? 'Downloads divided by Apple-reported discovery impressions.' : 'Available only after Apple delivers both Downloads and Discovery & Engagement reports.'} /><StatusCard label="IMPRESSIONS" value={metricAvailable('impressions') ? formatUnits(analytics.impressions) : '—'} note={metricAvailable('impressions') ? `Discovery & Engagement · processed ${formatDate(reportCoverage.engagement?.processingDate)}` : reportCoverage.engagement?.message || 'Apple Discovery & Engagement report required'} /><StatusCard label="PRODUCT PAGE VIEWS" value={metricAvailable('productPageViews') ? formatUnits(analytics.productPageViews) : '—'} note={metricAvailable('productPageViews') ? 'Apple-reported product page views in the analytics window.' : reportCoverage.engagement?.message || 'Apple Discovery & Engagement report required'} /><StatusCard label="UPDATES" value={metricAvailable('updates') ? formatUnits(analytics.updates) : '—'} note={metricAvailable('updates') ? 'Manual and automatic updates reported by Apple.' : reportCoverage.downloads?.message || 'Apple Downloads report required'} /><StatusCard label="APP STORE RATING" value={metrics.reviews?.averageRating ? `${metrics.reviews.averageRating} / 5` : '—'} note={metrics.reviews?.sampledReviewCount ? `${metrics.reviews.sampledReviewCount} authorized review records sampled` : metrics.availability?.reviews?.message || 'No review records returned by Apple'} /><StatusCard label="LATEST VERSION" value={release?.latestAppStoreVersion?.versionString || metrics.catalog?.latestVersion || '—'} note={release?.latestAppStoreVersion?.appStoreState || metrics.catalog?.latestVersionState || metrics.availability?.release?.message || 'No App Store version returned'} /><StatusCard label="EST. PROCEEDS" value={sales?.status === 'available' ? proceeds.value : '—'} note={sales?.status === 'available' ? proceeds.note : metrics.availability?.proceeds?.message || 'Vendor number and Sales report required'} /></section> : <section className="studio-panel insights-analytics-blocker" style={{ marginTop:16, padding:22 }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>APPLE ANALYTICS / SETUP STATE</div><h2 style={{ color:'#ffffff', fontSize:24, marginTop:7 }}>Apple reporting is not ready for this app yet.</h2><p style={{ color:'rgba(240,240,240,.64)', fontSize:12, lineHeight:1.65, marginTop:8, maxWidth:760 }}>{metrics.availability?.analytics?.message || 'FloStudio will show downloads, redownloads, discovery, product-page views, updates, subscriptions, and conversion only after Apple provides authorized report instances.'}</p><div className="insights-analytics-blocker__steps"><article><span>01</span><div><b>Apple authorization</b><p>{analytics?.status === 'requires_admin_analytics_request' ? 'An Admin Team API key is required once to request this app’s first ongoing Analytics Reports feed.' : 'The existing Apple key must be authorized to read the requested report type.'}</p></div></article><article><span>02</span><div><b>Apple report processing</b><p>After an ongoing request is accepted, Apple usually needs 24–48 hours before first downloadable report instances appear.</p></div></article><article><span>03</span><div><b>FloStudio reporting</b><p>Once Apple returns a report, only that returned stream appears—never an estimated zero or filler metric.</p></div></article></div></section>}
      {analyticsReady && <section className="studio-panel" style={{ marginTop:16, padding:20, borderColor:'rgba(197,197,197,.25)' }}><div style={{ display:'grid', gridTemplateColumns:'minmax(0,1.1fr) minmax(260px,.9fr)', gap:18, alignItems:'start' }}><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>APPLE REPORT INTERPRETATION</div><h2 style={{ color:'#fff', fontSize:21, marginTop:6 }}>{analyticsReady ? (analyticsHasActivity ? 'Apple returned measurable activity for this app.' : 'Apple returned reporting, but no activity rows in the current window.') : 'Apple has not supplied a usable acquisition report for this app yet.'}</h2><p style={{ color:'rgba(240,240,240,.63)', fontSize:11.5, lineHeight:1.6, marginTop:7 }}>{analyticsReady ? `The cards above reflect Apple’s ${analytics.periodDays}-day report window. A displayed 0 is an Apple-reported zero, not a FloStudio estimate. ${analytics.processingDates?.downloads || analytics.processingDates?.engagement ? `Latest report processing dates: downloads ${analytics.processingDates?.downloads || 'not supplied'} · engagement ${analytics.processingDates?.engagement || 'not supplied'}.` : 'Apple returned the report type but did not attach a processing date for every data stream.'}` : metrics.availability?.analytics?.message || 'Use the selected app’s connection panel to request or authorize the required Apple Analytics reports.'}</p></div><div style={{ padding:'12px', border:'1px solid rgba(240,240,240,.12)', background:'rgba(18,18,18,.3)' }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>PUBLIC APP STORE CONTEXT</div><div style={{ display:'flex', gap:10, marginTop:8, alignItems:'center' }}>{sourceFacts.image ? <img src={sourceFacts.image} alt="Selected app artwork" style={{ width:46, height:46, objectFit:'cover', borderRadius:11, border:'1px solid rgba(240,240,240,.15)' }} /> : null}<div><b style={{ color:'#fff', fontSize:12 }}>{sourceFacts.storeMetadata?.trackName || metrics.catalog?.name || selectedApp?.name}</b><p style={{ color:'rgba(240,240,240,.58)', fontSize:10.5, marginTop:3 }}>{sourceFacts.category || sourceFacts.storeMetadata?.primaryGenreName || 'App Store listing context'} · {(sourceFacts.screenshots || []).length} saved screenshot{(sourceFacts.screenshots || []).length === 1 ? '' : 's'}</p></div></div><p style={{ color:'rgba(240,240,240,.52)', fontSize:10, lineHeight:1.45, marginTop:9 }}>{sourceFacts.storeMetadata?.sellerName ? `Seller: ${sourceFacts.storeMetadata.sellerName}` : 'Public listing data is retained separately from Apple’s private reporting feeds.'}</p></div></div></section>}
      <details className="insights-report-fold"><summary>View Apple report coverage, processing dates, and stream availability</summary><section className="studio-panel insights-coverage-console" style={{ marginTop:10, padding:20, borderColor:'rgba(197,197,197,.25)' }}><div style={{ display:'flex', justifyContent:'space-between', alignItems:'start', gap:18, flexWrap:'wrap' }}><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>ANALYTICS COVERAGE / APPLE REPORTS API</div><h2 style={{ color:'#ffffff', fontSize:21, marginTop:6 }}>Every reporting stream has its own Apple state.</h2><p style={{ color:'rgba(240,240,240,.6)', fontSize:11.5, lineHeight:1.55, marginTop:5, maxWidth:760 }}>{analytics?.requestDiagnostics ? `${analytics.requestDiagnostics.activeRequestCount} active ongoing request${analytics.requestDiagnostics.activeRequestCount === 1 ? '' : 's'} found; ${analytics.requestDiagnostics.readableRequestCount} readable by this key.` : metrics.availability?.analytics?.message || 'FloStudio will retain the exact Apple report state after the next secure sync.'}</p></div><div style={{ padding:'10px 12px', border:'1px solid rgba(240,240,240,.12)', background:'rgba(21,21,21,.3)', minWidth:190 }}><div className="studio-kicker" style={{ color:'var(--signal)', fontSize:8 }}>ANALYTICS WINDOW</div><b style={{ display:'block', color:'#fff', fontSize:15, marginTop:5 }}>{analytics?.periodDays ? `${analytics.periodDays} days` : 'Apple pending'}</b><p style={{ color:'rgba(240,240,240,.55)', fontSize:10, marginTop:3 }}>Ongoing daily report instances are normalized per data stream.</p></div></div><div className="insights-coverage-grid" style={{ display:'grid', gridTemplateColumns:'repeat(5,minmax(0,1fr))', gap:9, marginTop:15 }}>{[['Downloads', reportCoverage.downloads], ['Discovery & engagement', reportCoverage.engagement], ['Subscription state', reportCoverage.subscriptionState], ['Subscription events', reportCoverage.subscriptionEvents], ['Purchases', reportCoverage.purchases]].map(([label, entry]) => <article key={label} className="insights-coverage-card"><div style={{ display:'flex', justifyContent:'space-between', gap:8, alignItems:'start' }}><b>{label}</b><AvailabilityBadge entry={entry} /></div><p>{entry?.reportName || 'Apple report type pending'}</p><small>{entry?.processingDate ? `Processed ${formatDate(entry.processingDate)}` : entry?.message || 'No current report detail returned.'}</small></article>)}</div></section></details>
      <section className="studio-panel" style={{ marginTop:16, padding:20, display:'flex', justifyContent:'space-between', alignItems:'end', gap:16, flexWrap:'wrap', borderColor:'rgba(197,197,197,.25)' }}><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>REPORTING HEALTH / SELECTED APP</div><b style={{ color:'#ffffff', display:'block', marginTop:5 }}>{metrics.catalog?.name || selectedApp?.name} · {metrics.catalog?.bundleId || 'bundle ID pending'}</b><span style={{ color:'rgba(240,240,240,.58)', fontSize:11 }}>Last secure sync: {connection.last_synced_at ? new Date(connection.last_synced_at).toLocaleString() : 'not available'}</span><label className="portfolio-field" style={{ display:'block', marginTop:13, minWidth:280 }}><span>Apple Vendor Number for {selectedApp?.name}</span><input value={vendorInput} onChange={event => setVendorInput(event.target.value)} placeholder="Reports → legal entity → Vendor #" /></label><p style={{ color:'rgba(240,240,240,.52)', fontSize:10.5, lineHeight:1.45, marginTop:6 }}>This applies only to the selected FloStudio app. Apple requires it to download that app’s Sales & Trends report.</p></div><div style={{ display:'flex', gap:8, flexWrap:'wrap' }}><button onClick={() => navigate(appStoreConnectPath(selectedApp?.id))} disabled={state.syncing} className="studio-button" style={{ background:'transparent', color:'var(--signal)', border:'1px solid rgba(197,197,197,.45)' }}>Edit / replace connection key →</button><button onClick={saveVendorAndSync} disabled={state.syncing || !vendorInput.trim()} className="studio-button">{state.syncing ? 'Pulling…' : 'Save & pull numbers →'}</button><button onClick={sync} disabled={state.syncing} className="studio-button" style={{ background:'transparent', color:'var(--signal)', border:'1px solid rgba(197,197,197,.45)' }}>{state.syncing ? 'Syncing…' : 'Pull latest data →'}</button></div></section>
      <section className="studio-panel" style={{ marginTop:16, padding:20, borderColor:'rgba(197,197,197,.25)' }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>SUBSCRIPTIONS / APPLE-REPORTED</div><h2 style={{ color:'#ffffff', fontSize:21, marginTop:6 }}>Recurring revenue and subscriber health.</h2><p style={{ color:'rgba(240,240,240,.6)', fontSize:11.5, lineHeight:1.55, marginTop:5, maxWidth:760 }}>{subscriptionsReady ? subscriptions.note : metrics.availability?.subscriptions?.message || 'Apple subscription reports are not available yet for this selected app.'}</p><div style={{ display:'grid', gridTemplateColumns:'repeat(3,minmax(0,1fr))', gap:12, marginTop:15 }}><StatusCard label="ACTIVE PAID PLANS" value={subscriptionsReady ? formatUnits(subscriptions.activePaidPlans) : '—'} note={subscriptionsReady ? `Subscription State · latest ${subscriptions.stateWindowDays}-day Apple snapshot` : 'Apple report pending'} highlight /><StatusCard label="FREE TRIALS" value={subscriptionsReady ? formatUnits(subscriptions.freeTrials) : '—'} note={subscriptionsReady ? 'Active trials in the Apple subscription state report.' : 'Apple report pending'} /><StatusCard label="RENEWALS" value={subscriptionsReady ? formatUnits(subscriptions.renewals) : '—'} note={subscriptionsReady ? `Apple subscription events · last ${subscriptions.eventWindowDays} days` : 'Apple report pending'} /><StatusCard label="TRIAL → PAID" value={subscriptionsReady ? formatUnits(subscriptions.trialToPaid) : '—'} note={subscriptionsReady ? `Apple subscription conversions · last ${subscriptions.eventWindowDays} days` : 'Apple report pending'} /><StatusCard label="VOLUNTARY CHURN" value={subscriptionsReady ? formatUnits(subscriptions.voluntaryChurn) : '—'} note={subscriptionsReady ? `Apple subscription events · last ${subscriptions.eventWindowDays} days` : 'Apple report pending'} /><StatusCard label="IN-APP PROCEEDS" value={subscriptionsReady ? formatUsd(subscriptions.inAppPurchaseProceedsUsd) : '—'} note={subscriptionsReady ? `Apple in-app purchase proceeds · last ${subscriptions.purchaseWindowDays} days · USD` : 'Apple report pending'} /></div></section>
      <section className="studio-panel" style={{ marginTop:16, padding:20, borderColor:'rgba(197,197,197,.25)' }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>STORE CONFIGURATION SNAPSHOT / SELECTED APP</div><h2 style={{ color:'#ffffff', fontSize:21, marginTop:6 }}>Live paywall and App Store settings.</h2><p style={{ color:'rgba(240,240,240,.6)', fontSize:11.5, lineHeight:1.55, marginTop:5 }}>{configurationReady ? configuration.message : metrics.availability?.configuration?.message || 'App Store configuration has not been returned for this selected app yet.'}</p>{configurationReady ? <><div className="insights-config-metrics" style={{ display:'grid', gridTemplateColumns:'repeat(4,minmax(0,1fr))', gap:10, marginTop:14 }}><StatusCard label="APPLE APP ID" value={configuration.app?.appleId || '—'} note="Selected app only" /><StatusCard label="BUNDLE ID" value={configuration.app?.bundleId || '—'} note={configuration.app?.primaryLocale || 'Locale not returned'} compactValue /><StatusCard label="SUBSCRIPTION GROUPS" value={formatUnits(configuration.subscriptionGroups?.length)} note="Configured in App Store Connect" /><StatusCard label="IN-APP PRODUCTS" value={formatUnits(configuration.inAppPurchases?.length)} note="Configured in App Store Connect" /><StatusCard label="STOREFRONT TERRITORIES" value={appAvailability ? formatUnits(appAvailability.availableTerritoryCount) : '—'} note={appAvailability ? `${boolLabel(appAvailability.availableInNewTerritories, 'New territories enabled', 'New territories not automatic')}${appAvailability.territoryListTruncated ? ' · first 12 retained below' : ''}` : 'Apple availability resource not returned'} /><StatusCard label="CURRENT U.S. PRICE" value={pricing?.status === 'available' ? formatApplePrice(pricing.usPrice) : '—'} note={pricing?.status === 'available' ? (pricing.usPrice ? `${pricing.usPriceSource === 'manual' ? 'Manual' : 'Automatic'} price · ${pricing.baseTerritory?.territory || 'base territory pending'}` : 'No currently effective United States price returned') : pricing?.message || 'Apple price schedule not returned'} /></div><details className="insights-detail-fold"><summary>More App Store configuration</summary><div className="insights-detail-fold__grid"><div><span>Next U.S. price</span><b>{pricing?.status === 'available' ? formatApplePrice(pricing.scheduledUsChanges?.[0]) : '—'}</b><small>{pricing?.scheduledUsChanges?.[0]?.startDate ? `Effective ${formatDate(pricing.scheduledUsChanges[0].startDate)}` : 'No scheduled United States price change returned'}</small></div><div><span>Age rating</span><b>{metadata?.appInfo?.appStoreAgeRating || '—'}</b><small>{metadata?.status === 'available' ? 'Apple App Information' : metadata?.message || 'Apple app information not returned'}</small></div><div><span>Store localizations</span><b>{metadata?.status === 'available' ? formatUnits((metadata.appInfoLocalizationCount || 0) + (metadata.versionLocalizationCount || 0)) : '—'}</b><small>{metadata?.status === 'available' ? `${metadata.appInfoLocalizationCount || 0} app info · ${metadata.versionLocalizationCount || 0} version` : 'Apple localization data not returned'}</small></div><div><span>Pricing source availability</span><b>{pricing?.status === 'available' ? 'Returned' : availabilityLabel(pricing)}</b><small>{pricing?.status === 'available' ? `Base territory: ${pricing.baseTerritory?.territory || 'not returned'}${pricing.baseTerritory?.currency ? ` · ${pricing.baseTerritory.currency}` : ''}` : pricing?.message || 'Apple price schedule pending'}</small></div></div></details><div className="insights-store-columns" style={{ display:'grid', gridTemplateColumns:'repeat(2,minmax(0,1fr))', gap:14, marginTop:16 }}><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>AUTO-RENEWABLE SUBSCRIPTIONS</div><div style={{ display:'grid', gap:9, marginTop:8 }}>{configuration.subscriptionGroups?.flatMap(group => group.subscriptions.map(subscription => <article key={subscription.id} className="insights-store-product" style={{ padding:12, border:'1px solid rgba(240,240,240,.12)', background:'rgba(21,21,21,.35)' }}><div className="insights-store-product__head" style={{ display:'flex', justifyContent:'space-between', gap:10 }}><b style={{ color:'#ffffff', fontSize:12 }}>{subscription.name}</b><span style={{ color:'var(--signal)', fontSize:12, fontWeight:800 }}>{subscription.price?.customerPrice ? `$${subscription.price.customerPrice} USD` : 'Price not returned'}</span></div><p style={{ color:'rgba(240,240,240,.57)', fontSize:10.5, marginTop:5 }}>{subscription.productId || 'Product ID not returned'} · {subscription.period || 'Period not returned'} · {subscription.state || 'State not returned'}{subscription.price?.preserved ? ' · Preserved price' : ''}</p>{subscription.introductoryOffers?.length ? <p style={{ color:'#d3d3d3', fontSize:10.5, marginTop:5 }}>{subscription.introductoryOffers.length} introductory offer{subscription.introductoryOffers.length === 1 ? '' : 's'} configured</p> : null}</article>)) || <p style={{ color:'rgba(240,240,240,.56)', fontSize:11 }}>No subscription products returned for this app.</p>}</div></div><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>IN-APP PURCHASE PAYWALL PRODUCTS</div><div style={{ display:'grid', gap:9, marginTop:8 }}>{configuration.inAppPurchases?.length ? configuration.inAppPurchases.map(purchase => <article key={purchase.id} className="insights-store-product" style={{ padding:12, border:'1px solid rgba(240,240,240,.12)', background:'rgba(21,21,21,.35)' }}><div className="insights-store-product__head" style={{ display:'flex', justifyContent:'space-between', gap:10 }}><b style={{ color:'#ffffff', fontSize:12 }}>{purchase.name}</b><span style={{ color:'var(--signal)', fontSize:12, fontWeight:800 }}>{purchase.price?.customerPrice ? `$${purchase.price.customerPrice} USD` : 'Price not returned'}</span></div><p style={{ color:'rgba(240,240,240,.57)', fontSize:10.5, marginTop:5 }}>{purchase.productId || 'Product ID not returned'} · {purchase.type || 'Type not returned'} · {purchase.state || 'State not returned'}{purchase.familySharable ? ' · Family sharing' : ''}</p></article>) : <p style={{ color:'rgba(240,240,240,.56)', fontSize:11 }}>No in-app purchase products returned for this app.</p>}</div></div></div></> : <p style={{ color:'rgba(240,240,240,.56)', fontSize:12, marginTop:14 }}>Use a Team API key with App Manager, Admin, or Account Holder access to retrieve the live configuration snapshot.</p>}</section>
      <section className="studio-panel" style={{ marginTop:16, padding:20, borderColor:'rgba(197,197,197,.25)' }}><div style={{ display:'flex', justifyContent:'space-between', gap:16, alignItems:'start', flexWrap:'wrap' }}><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>RELEASE & TESTFLIGHT READINESS / SELECTED APP</div><h2 style={{ color:'#ffffff', fontSize:21, marginTop:6 }}>Current App Store release and recent build state.</h2><p style={{ color:'rgba(240,240,240,.6)', fontSize:11.5, lineHeight:1.55, marginTop:5, maxWidth:760 }}>{release?.message || metrics.availability?.release?.message || 'FloStudio will load release and TestFlight readiness on the next secure Apple sync.'}</p></div><div style={{ display:'flex', gap:7, flexWrap:'wrap' }}><AvailabilityBadge entry={metrics.availability?.release} /><AvailabilityBadge entry={metrics.availability?.testFlight} /></div></div><div className="insights-release-grid" style={{ display:'grid', gridTemplateColumns:'repeat(4,minmax(0,1fr))', gap:10, marginTop:15 }}><StatusCard label="APP STORE VERSION" value={release?.latestAppStoreVersion?.versionString || '—'} note={formatApplePlatform(release?.latestAppStoreVersion?.platform)} /><StatusCard label="RELEASE STATE" value={formatAppleState(release?.latestAppStoreVersion?.appStoreState)} note={release?.latestAppStoreVersion?.releaseDate ? `Released ${formatDate(release.latestAppStoreVersion.releaseDate)}` : release?.latestAppStoreVersion?.earliestReleaseDate ? `Earliest release ${formatDate(release.latestAppStoreVersion.earliestReleaseDate)}` : 'Release date not supplied'} /><StatusCard label="RECENT BUILDS" value={testFlight?.status === 'available' ? formatUnits(testFlight.recentBuildCount) : '—'} note={testFlight?.status === 'available' ? 'Ten most recent Apple builds retained' : testFlight?.message || 'Apple build access needed'} /><StatusCard label="TESTFLIGHT GROUPS" value={testFlight?.status === 'available' ? formatUnits(testFlight.betaGroupCount) : '—'} note={testFlight?.status === 'available' ? `${testFlight.internalBetaGroupCount || 0} internal · ${testFlight.externalBetaGroupCount || 0} external · ${testFlight.publicBetaLinkCount || 0} public links` : 'Apple beta-group access needed'} /></div>{testFlight?.status === 'available' && testFlight.recentBuilds?.length ? <div className="insights-build-list" style={{ display:'grid', gap:8, marginTop:15 }}>{testFlight.recentBuilds.map(build => <article key={build.id} className="insights-build-row"><div><b>Build {build.buildNumber || '—'}{build.prereleaseVersion ? ` · ${build.prereleaseVersion}` : ''}</b><p>{build.platform || 'Platform not supplied'} · uploaded {formatDate(build.uploadedDate, { includeTime:true })} · {build.minOsVersion ? `min OS ${build.minOsVersion}` : 'minimum OS not supplied'}</p></div><div className="insights-build-row__states"><span>{build.processingState ? formatAppleState(build.processingState) : 'Processing state pending'}</span>{build.internalBuildState && <span>Internal: {formatAppleState(build.internalBuildState)}</span>}{build.externalBuildState && <span>External: {formatAppleState(build.externalBuildState)}</span>}{build.betaReviewState && <span>Beta review: {formatAppleState(build.betaReviewState)}</span>}{build.expired && <span>Expired</span>}</div></article>)}</div> : <p style={{ color:'rgba(240,240,240,.56)', fontSize:11.5, lineHeight:1.55, marginTop:15 }}>{testFlight?.status === 'available' ? 'Apple returned no recent builds for this app.' : 'FloStudio does not estimate build or TestFlight status when the authorized key has not returned it.'}</p>}</section>
      <section className="studio-panel" style={{ marginTop:16, padding:20, borderColor:'rgba(197,197,197,.25)' }}><div style={{ display:'flex', justifyContent:'space-between', gap:16, alignItems:'start', flexWrap:'wrap' }}><div><div className="studio-kicker" style={{ color:'var(--signal)' }}>APP STORE LISTING & LOCALIZATION / SELECTED APP</div><h2 style={{ color:'#ffffff', fontSize:21, marginTop:6 }}>Live customer-facing metadata returned by Apple.</h2><p style={{ color:'rgba(240,240,240,.6)', fontSize:11.5, lineHeight:1.55, marginTop:5, maxWidth:760 }}>{metadata?.message || metrics.availability?.localizedMetadata?.message || 'FloStudio will retain App Store metadata on the next secure Apple sync.'}</p></div><AvailabilityBadge entry={metrics.availability?.localizedMetadata} /></div><div className="insights-metadata-grid" style={{ display:'grid', gridTemplateColumns:'repeat(2,minmax(0,1fr))', gap:12, marginTop:15 }}><div className="insights-metadata-panel"><div className="studio-kicker">APP INFORMATION LOCALIZATIONS</div>{metadata?.appInfoLocalizations?.length ? <div style={{ display:'grid', gap:8, marginTop:9 }}>{metadata.appInfoLocalizations.map(localization => <article key={localization.id} className="insights-metadata-row"><b>{localization.locale || 'Locale pending'} · {localization.name || 'Name not supplied'}</b><p>{localization.subtitle || 'No subtitle returned'}</p>{localization.privacyPolicyUrl ? <a href={localization.privacyPolicyUrl} target="_blank" rel="noreferrer">Privacy policy ↗</a> : <small>Privacy-policy URL not supplied</small>}</article>)}</div> : <p>No app-information localizations returned by Apple.</p>}</div><div className="insights-metadata-panel"><div className="studio-kicker">CURRENT VERSION LOCALIZATIONS</div>{metadata?.versionLocalizations?.length ? <div style={{ display:'grid', gap:8, marginTop:9 }}>{metadata.versionLocalizations.map(localization => <article key={localization.id} className="insights-metadata-row"><b>{localization.locale || 'Locale pending'} · release copy</b><p>{localization.promotionalText || localization.whatsNew || localization.versionDescription || 'No promotional text or what’s new returned'}</p><small>{localization.keywords ? `Keywords: ${localization.keywords}` : 'Keywords not supplied'}{localization.supportUrl ? ' · support URL configured' : ''}{localization.marketingUrl ? ' · marketing URL configured' : ''}</small></article>)}</div> : <p>No current-version localizations returned by Apple.</p>}</div></div></section>
      <section style={{ display:'grid', gridTemplateColumns:'minmax(0,1.1fr) minmax(290px,.9fr)', gap:16, marginTop:16 }}><div className="studio-panel" style={{ padding:20 }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>CUSTOMER FEEDBACK / AUTHORIZED APP STORE REVIEWS</div><h2 style={{ color:'#ffffff', fontSize:21, marginTop:6 }}>What reviewers are saying.</h2>{metrics.reviews?.latest?.length ? <div style={{ display:'grid', gap:10, marginTop:14 }}>{metrics.reviews.latest.map(review => <article key={review.id} style={{ padding:12, background:'rgba(21,21,21,.45)', border:'1px solid rgba(240,240,240,.11)' }}><div style={{ display:'flex', justifyContent:'space-between', gap:10, color:'var(--signal)', fontSize:11, fontWeight:800 }}><span>{review.rating ? `${review.rating} / 5` : 'Rating not returned'}</span><span style={{ color:'rgba(240,240,240,.46)', fontWeight:500 }}>{review.createdDate ? new Date(review.createdDate).toLocaleDateString() : ''}</span></div>{review.title && <b style={{ color:'#ffffff', display:'block', fontSize:12, marginTop:6 }}>{review.title}</b>}{review.body && <p style={{ color:'rgba(240,240,240,.7)', lineHeight:1.5, fontSize:11.5, marginTop:5 }}>{review.body}</p>}</article>)}</div> : <p style={{ color:'rgba(240,240,240,.58)', fontSize:12, lineHeight:1.6, marginTop:13 }}>Apple did not return review records for this authorized key/app combination.</p>}</div><div className="studio-panel" style={{ padding:20 }}><div className="studio-kicker" style={{ color:'var(--signal)' }}>DATA READINESS</div><h2 style={{ color:'#ffffff', fontSize:20, marginTop:6 }}>What Apple has made available.</h2><div style={{ display:'grid', gap:9, marginTop:15 }}>{[['Catalog', { status:'available', message:'App identity returned directly from App Store Connect.' }], ['Store configuration', metrics.availability?.configuration], ['Price schedule', pricing], ['Store availability', appAvailability ? { status:'available', message:`${appAvailability.availableTerritoryCount} territory records returned by Apple.` } : { status:configuration?.sourceAvailability?.availability || 'pending', message:'Apple availability territories have not been returned.' }], ['Localized metadata', metrics.availability?.localizedMetadata], ['Release metadata', metrics.availability?.release], ['TestFlight', metrics.availability?.testFlight], ['App Analytics', metrics.availability?.analytics], ['Downloads', metrics.availability?.downloads], ['Discovery & engagement', metrics.availability?.engagement], ['Reviews', metrics.availability?.reviews], ['Proceeds', metrics.availability?.proceeds], ['Subscriptions', metrics.availability?.subscriptions]].map(([label, entry]) => <div key={label} style={{ padding:'9px 10px', border:'1px solid rgba(240,240,240,.12)', background:'rgba(21,21,21,.35)' }}><div style={{ display:'flex', justifyContent:'space-between', gap:8, alignItems:'start' }}><b style={{ color:'#ffffff', fontSize:11 }}>{label}</b><span style={{ color:entry?.status === 'available' ? 'var(--signal)' : '#c5c5c5', fontSize:9, textTransform:'uppercase', letterSpacing:'.06em', textAlign:'right' }}>{availabilityLabel(entry)}</span></div>{entry?.message && <p style={{ color:'rgba(240,240,240,.52)', fontSize:10, lineHeight:1.4, marginTop:4 }}>{entry.message}</p>}</div>)}</div></div></section>
    </>}
    {state.error && <div style={{ marginTop:14, padding:11, border:'1px solid rgba(135,135,135,.45)', background:'rgba(44,44,44,.28)', color:'#d9d9d9', fontSize:12 }}>{state.error}</div>}
    {state.notice && <div style={{ marginTop:14, padding:11, border:'1px solid rgba(197,197,197,.35)', background:'rgba(197,197,197,.08)', color:'#f0f0f0', fontSize:12 }}>{state.notice}</div>}
  </div></Layout>
}
